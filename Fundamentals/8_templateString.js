const name = 'Dante'
const conc = 'Hello ' + "Dante " + "1!"
const template = `
    Hello
    ${name}!
    How are you?
`
console.log(conc)
console.log(template)

console.log(`1 + 1 = ${1 + 1}`)

const up = (text) => text.toUpperCase()
console.log(`Hey ... ${up('take care')}`)

