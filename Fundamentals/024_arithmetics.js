const [a, b, c, d] = [1, 2, 3, 4]

const soma = a + b + c + d
const substracao = a - b - c - d
const multiplicacao = a * b * c * d
const divisao = a / b / c / d
const modulo = a % 2

console.log(soma, substracao, multiplicacao, Math.floor(divisao), modulo)